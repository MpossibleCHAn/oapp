import * as React from "react";

const Home = () => {
  return (
    <div>
      <div>Home</div>
      <button onClick={() => console.log("hello")}>click</button>
    </div>
  );
};

export default Home;
